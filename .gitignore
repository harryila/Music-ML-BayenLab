# Virtual environments
venv/
venv311/
venv_mt3/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/

# Model checkpoints (too large for git — download separately)
MIDI2ScoreTransformer/checkpoints/
checkpoints_backup/
mt3/checkpoints/
hFT-Transformer/checkpoint/

# Output artifacts (regenerable)
outputs/
midi/

# Benchmark MIDI files (from MAESTRO, copyrighted — download via catalog.json)
benchmark/*/*.midi
benchmark/*/path_b/
benchmark/*/path_c/

# Synthetic pretrain bulk — all regenerable from scripts/
data/pairs/
data/pairs_deduped_*/
data/cache_pdmx/
data/cache_pdmx_*/
data/pdmx_piano_subset.csv
data/pdmx_piano_subset.deduped.csv
data/render_test/

# ASAP parse-cache (regenerable via build_asap_cache.py / dataset.py; ~5MB of pkls)
MIDI2ScoreTransformer/data/cache/

# ASAP dataset clone (regenerable: clone TimFelixBeyer/asap-dataset at the
# pinned commit, see MIDI2ScoreTransformer/README.md)
MIDI2ScoreTransformer/data/asap-dataset/

# Audio preprocessing artifacts (regenerable via --normalize-audio)
*.pre.wav

# Empty pretrain eval outputs (the from-zero-generation issue is documented;
# see SYNTHETIC_PRETRAIN_STATUS.md)
benchmark/pretrain_eval/

# OS
.DS_Store

# IDE
.cursor/
benchmark/decomp_work/
benchmark/tier2_out/

# Claude Code agent state (not project code)
.claude/

# Large regenerable corpus manifests (data lists w/ box paths; rebuild via scripts/make_pairs.py, build_*.py)
data/pairs_classical_*_manifest.csv
data/pairs_classical_*_tuplrate.csv
data/pairs_st_merged_manifest.csv
data/pairs_gentle5k_manifest.csv
data/pdmx_*_subset.csv
